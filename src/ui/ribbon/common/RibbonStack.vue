<template>
  <div class="ribbon-stack">
    <slot />
  </div>
</template>

<script setup lang="ts">
/**
 * מחסנית אנכית של פקדים קטנים לצד פקד ראשי — הדפוס של Word, ושל „בית”.
 *
 * ## למה קומפוננטה ולא מחלקה
 *
 * זה היה `.column-items`, ואותן חמש שורות CSS בדיוק הועתקו לארבעה בלוקים
 * `scoped` נפרדים (בית, סקירה, תצוגה, אוצריא). הקיבוץ שכל שאר הלשוניות עברו
 * היה מכפיל אותן לשמונה. מחלקה משוכפלת אין לה בעלים, ואי-אפשר לשנות בה מרווח
 * בלי לחפש את כל העותקים — כפילות שהיא גם החלטת עיצוב.
 *
 * ## תקרת השלושה
 *
 * המחסנית מחזיקה **עד שלושה** פקדים, וזה אינו נוהג אלא החוזה שכל גובה הרצועה
 * נשען עליו: `--ribbon-content-h` הוא במדויק גם גובה כפתור גדול וגם גובה
 * שלוש שורות קטנות (tokens.css), ולכן מחסנית רביעית גולשת מהקבוצה.
 *
 * זה בדיוק מה שקרה לפני התיקון: „שינויים” ב„סקירה” החזיקה ארבעה, הרצועה שם
 * יצאה 126px מול 96px בשאר הלשוניות, והמסמך קפץ 30px בכל החלפת לשונית.
 * התקרה נשמרת ב-tests/unit/ribbon-geometry.test.ts, כדי שהיא לא תחזור להיות
 * דבר שצריך לזכור.
 *
 * `justify-content: center` ולא `flex-start`: מחסנית של שניים מתמרכזת מול
 * הכפתור הגדול שלצידה, במקום להיתלות מלמעלה.
 */
</script>

<style scoped>
.ribbon-stack {
  display: flex;
  flex-direction: column;
  gap: var(--ribbon-row-gap);
  justify-content: center;
  /* לא לסמוך רק על min-width:max-content שבקבוצה — מחסנית מכווצת נשפכת על שכנתה. */
  flex-shrink: 0;
}

/* `RibbonMenuButton` עוטף את הכפתור שלו ב-`.ribbon-menu` (מיכל למיקום
   הפופאובר), ולכן הכפתור אינו ילד ישיר של המחסנית ואינו נמתח איתה. בלי זה
   תפריט במחסנית היה צר משכניו — הכפתורים הישירים נמתחים ל-`stretch`, והוא לא. */
.ribbon-stack :deep(.ribbon-menu) {
  align-self: stretch;
}

.ribbon-stack :deep(.ribbon-menu > .word-btn) {
  width: 100%;
}
</style>
